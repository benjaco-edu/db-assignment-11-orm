{
  "name": "db-11-orm",
  "version": "1.0.0",
  "description": "To run the code, you have to have typescript installed \"npm install -g typescript\", \"npm install -g ts-node\" and \"npm i @types/node\"",
  "main": "index.js",
  "dependencies": {
    "mysql2": "^1.6.5"
  },
  "devDependencies": {
    "@types/mysql2": "github:types/mysql2",
    "@types/node": "^8.0.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
